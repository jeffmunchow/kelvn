/**
 * newsletter.js — redirecionado para album-revisao.js via vercel.json.
 * Este arquivo não exporta um handler válido para não consumir slot de função Vercel.
 * As rotas /api/newsletter são capturadas pelo rewrite em vercel.json antes
 * de chegarem aqui e encaminhadas para /api/album-revisao.
 */
module.exports = null;
